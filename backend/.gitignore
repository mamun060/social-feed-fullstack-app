# --- General ---
.DS_Store
Thumbs.db
desktop.ini

# --- Editors and IDEs ---
# VS Code
.vscode/
*.code-workspace

# JetBrains (PyCharm, IntelliJ)
.idea/
*.iml

# Sublime Text
*.sublime-project
*.sublime-workspace

# --- Python Environments ---
# Virtual Environments (common names)
venv/
env/
.venv/
.env/
venv.bak/
my_env/
py_env/

# Pipenv
Pipfile.lock

# Poetry
poetry.lock

# --- Python Compilation and Cache ---
__pycache__/
*.py[cod]
*$py.class

# --- Django ---
# Local database (if you switch back temporarily)
db.sqlite3
db.sqlite3-journal

# Local environment settings file (Contains secrets! NEVER commit this)
.env
.env.*
! .env.example

# Django debug toolbar panels
__pycache__/_dt_*

# Collected static files (usually generated on deployment)
staticfiles/
static_root/

# User-uploaded media files (should be stored in cloud storage in production)
media/
media_root/

# Sphinx documentation builds
docs/_build/

# Pytest
.pytest_cache/

# Coverage reports
htmlcov/
.coverage
.coverage.*
coverage.xml

# Mypy type checking
.mypy_cache/
.dmypy.json
dmypy.json

# Celery
celerybeat-schedule
celerybeat.pid

# --- Jupyter Notebooks (if used) ---
.ipynb_checkpoints
*/.ipynb_checkpoints/*
profile_default/
ipython_config.py

# --- Docker (Optional, if you use Docker later) ---
# .dockerignore is usually used for Docker build context,
# but it's good practice to ignore local docker files in git too.
docker-compose.override.yml
.docker/